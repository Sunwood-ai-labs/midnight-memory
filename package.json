{
  "name": "midnight-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "test:viewer": "uv run python scripts/prepare_viewer_test_assets.py && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "vitepress": "^1.6.4"
  }
}
