interface:
  display_name: "Midnight Memory Subtitle Sections"
  short_description: "Read the repository root skill and use the midnight-memory workflow from there."
  default_prompt: "Read the repository root ./SKILL.md and use it as the canonical workflow guide for midnight-memory tasks."
